// program to multiply two 2D array if possible

#include <stdio.h>
int main()
{
    int i, j;
    int n1, m1, n2, m2;
    printf("Enter number of rows and columns of first array: ");
    scanf("%d %d", &n1 , &m1);
    printf("Enter elements of the first array: \n");
    int arr1[n1][m1];
    for(i=0; i<n1; i++)
    {
        for(j=0; j<m1; j++)
            scanf("%d", &arr1[i][j]);
    }
    printf("\nEnter number of rows and columns of second array: ");
    scanf("%d %d", &n2, &m2);
    int arr[n2][m2];
    for(i=0; i<n1; i++)
        for(j=0; j<m1; j++)
            scanf("%d", &arr2[i][j]);

    for(i=0; i<m1; i++)
        for(j=)
    return 0;
}



